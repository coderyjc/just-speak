# 纯 API 语音转文字工具：第一版实施任务书

本文件可直接交给 Codex 执行。目标是交付可在 Windows 上实际运行的第一版应用。请按下述顺序完成实现、验证和使用说明；常规技术细节自行决定，避免停留在方案讨论。

文档日期：2026-09-10。以下 API 事实已对照官方文档核验，实际开发时应再次确认所安装 SDK 与目标账号的模型可用性。

## 1. 用户需求与第一版边界

用户使用 Windows，电脑无法承担本地 ASR 模型推理，只接受免费开源客户端，可以自行填写云端 ASR API 密钥并承担模型调用费用。

必须完整实现两种输入方式：

| 场景 | 第一版要求 |
| --- | --- |
| 麦克风实时转写 | 单击开始、单击停止；讲话过程中持续显示文字；连续讲话超过 2 分钟；以连续录音 60 分钟作为验收场景，软件不设置 2 分钟或 60 分钟自动停止限制 |
| 音视频文件转写 | 选择音频或视频，自动提取音轨、分段提交云端、按原顺序合并；支持至少 60 分钟的文件 |
| 音频提取 | 可以单独把视频中的音频保存成 WAV，用户无需手动操作 FFmpeg |
| 结果保存 | 自动保存原始识别结果；可编辑最终文本、复制全文、导出 UTF-8 TXT |
| 失败恢复 | 网络中断期间保留录音；文件任务支持失败重试和重启后继续；未完成部分有明确标记 |

识别全部在云端进行。本机仅运行界面、录音、音轨提取、重采样、磁盘保存和网络请求。不得引入本地 Whisper、FunASR 推理框架、PyTorch、CUDA、模型权重下载或自部署模型服务器。

第一版不做系统声音采集、全局输入法注入、说话人分离、AI 润色、翻译、字幕精细编辑、账号系统、云同步、开机启动或后台常驻。使用中文界面；以普通话为主，允许用户选择英文或自动识别。中文夹杂英文的效果以实际模型表现为准。

## 2. 已确定的技术路线

采用 **Python 桌面客户端 + 阿里云百炼云端 ASR**。第一版只接通百炼这一家服务商，两个输入场景共用一个后端适配模块。

| 部分 | 选型 |
| --- | --- |
| Python | 3.12，项目独立虚拟环境，验证后锁定依赖版本 |
| 桌面界面 | PySide6，普通窗口与控件即可 |
| 音频采集 | sounddevice；音量计算可使用 NumPy |
| 音频转换 | FFmpeg + ffprobe，通过 subprocess 参数列表调用 |
| 云端 ASR | 官方 dashscope SDK 的 dashscope.audio.asr.Recognition |
| 持久化 | Python 标准库 sqlite3 + 本地音频文件 |
| 密钥保存 | keyring，使用 Windows 凭据存储；也允许仅本次会话使用 |
| 必要测试 | pytest + 可注入的模拟 ASR 后端 |

这里安装的 `dashscope` 是云端调用 SDK，不包含本地 ASR 模型推理。

默认模型使用 `fun-asr-realtime`，模型名可配置；同协议的 `qwen-audio-3.0-asr-flash-streaming` 可作为候选。不能假定任意模型名或任意 OpenAI 兼容地址都能使用此适配器。

### 一套 SDK 覆盖两类输入

官方 Recognition 类提供两种入口：[1]

- 实时：`start()` → 多次 `send_audio_frame()` → `stop()`，通过回调接收文字。
- 文件：`call(本地音频文件路径)`，SDK 向云端发送音频并返回结果；必须在后台执行。

文件模式将长音频切成短 WAV，再逐段调用同一个云端服务。这样第一版无需配置对象存储、公网音频 URL 或另一家服务商。该模式仍然需要联网。

不要把其他 Filetrans 接口的“12 小时 / 2 GB”限制套用到这里。这里的一小时文件能力由客户端分段处理实现。文件处理速度需实测，不承诺固定倍速。

## 3. 配置与界面

采用四个简单页面或标签：实时录音、文件转写、历史记录、设置。界面优先清晰、可操作，不做复杂动画。

| 设置 | 默认或行为 |
| --- | --- |
| API Key | 密码框；可选择保存到 Windows 凭据存储 |
| 地域 | 北京；可切换新加坡，提醒密钥与地域匹配 |
| Workspace ID | 可选，用于生成业务空间专属地址 |
| WebSocket 地址 | 根据地域生成；高级设置允许修改完整地址 |
| 模型名 | fun-asr-realtime |
| 语言 | 中文、英文、自动；按模型支持的参数映射 |
| 麦克风 | 枚举设备，可选择并查看音量 |
| 数据目录 | 优先 D:\ASR-Client\data；D 盘不可用时让用户选择可写目录 |
| FFmpeg 路径 | 优先显式配置，其次项目 bin 目录，再查 PATH |
| 文件片段目标长度 | 60 秒；高级设置可改为 30～120 秒 |

旧公共地址目前仍可用，第一版可用北京公共 WebSocket 地址作为无需 Workspace ID 的预设；配置中保留专属地址支持。提交代码前根据官方文档核验并在 README 记录：[1][2]

- 北京公共地址：`wss://dashscope.aliyuncs.com/api-ws/v1/inference`
- 北京专属地址：`wss://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference`
- 新加坡公共地址：`wss://dashscope-intl.aliyuncs.com/api-ws/v1/inference`
- 新加坡专属地址：`wss://{WorkspaceId}.ap-southeast-1.maas.aliyuncs.com/api-ws/v1/inference`

配置修改对新任务生效，进行中的任务使用启动时的配置快照。尤其注意 SDK 全局配置变量，不能在任务运行中改变其密钥或地址。

提供“测试连接”：先检查配置，然后让用户录制约 3～5 秒并实际提交 ASR，明确显示测试结果。仅连上 WebSocket 不算模型调用成功。没有密钥时，界面与本地功能仍可启动使用。

## 4. 实时录音流程

### 音频与线程

1. 单击开始后创建会话，打开麦克风，持续保存音频并启动云端识别。
2. 统一发送 16 kHz、单声道、16-bit little-endian PCM。建议按约 100 ms 分包，即 1600 个采样点、3200 字节；单次会话持续发送这些帧，100 ms 不限制总讲话时长。[1]
3. 设备不支持 16 kHz 时，按其支持的采样率采集，使用有连续状态的重采样或 FFmpeg 管道转换；不能只修改采样率标签。48 kHz 和 44.1 kHz 输入均须验证时长正确。
4. 音频回调只复制数据到有界队列；网络请求、文件写入、重采样和界面刷新由后台工作单元处理。
5. 保存线程持续把标准 PCM 写入磁盘，网络发送从已保存音频中读取。保存速度与网络速度解耦，网络积压保存在磁盘上。检查 SDK 的内部发送缓存，避免把全部积压音频快速塞入其队列；发送持续落后时转入缺口补转写流程。
6. 采用后台工作对象与 Qt 信号更新界面，不得从 SDK 回调线程直接修改控件。[4][5]

原始录音可以采用连续 PCM 文件加元数据，结束后封装成 WAV。元数据必须记录采样率、声道、位深与已保存采样数，异常退出后能根据实际文件长度恢复已落盘的完整采样。

所有音频时间位置以整数采样数为准，服务端时间戳映射回本地会话时间轴；不要使用系统墙上时钟推断音频位置。

### 文字显示与正常停止

- 显示录音时长、音量、网络状态、已确认文字和末尾临时文字。
- 临时结果原位更新；收到最终结果后提交一次，禁止把每次修订都追加成新段落。
- 使用本地流会话编号加服务端句子标识建立稳定身份；若 SDK 未给句子 ID，依据已核实的句子时间位置维护身份。
- 文件返回可能是句子列表，实时回调可能是单句对象；适配层统一解析，不在 UI 散落兼容判断。[1]
- 保留语音停顿对应的音频帧，使用模型支持的心跳配置维持静音期间连接。第一版不安装本地神经网络 VAD。[1]
- 单击停止后，先停止采集、写完剩余音频、发送已保存但未发送的尾部，再请求正常结束，等待最后的最终结果。
- `stop()` 可能阻塞，必须在后台执行；设置有界等待，例如 15 秒。超时后保存任务为“待补转写”，不能直接丢弃最后一句或显示完成。
- 迟到回调必须检查会话身份与任务状态，不能更新已经关闭或被后续结果替代的任务。

正常联网时，以开口后约 1～3 秒出现文字作为体验目标；记录实测延迟，云端和网络延迟不作为应用自身能绝对保证的指标。

## 5. 网络中断与补转写

这是第一版必做能力，但采用简单明确的恢复方式。

1. 网络失败时继续保存录音，界面显示“录音继续，云端暂不可用”。已确认文字保留，临时文字标为未确认。
2. 记录最后一个已持久化最终句子的结束采样位置，以及新流重新开始的采样位置；两者之间形成待补转写区间。
3. 对暂时性错误按退避策略尝试恢复连接，例如 1、2、4、8 秒；不得忙循环。重连后开始新的流会话，恢复当前讲话的实时显示。
4. 第一版在停止录音后自动补转写缺口，复用文件分段流程。录音期间不同时启动批量补转写，以简化并发与资源管理。
5. 补转写按音频区间写入对应位置；最终文稿按时间轴重建。重试、重复回调和旧连接迟到事件不会生成重复段落。
6. 断点附近可带少量上下文音频，合并时依靠真实时间戳与重叠区域对齐。无法确认的衔接保留原结果供复查，不做全局文本去重。
7. 若模型未给可用的最终时间戳，保守地把整个受影响流会话作为待重转范围；重转完成后替换该范围结果，避免猜测缺口位置。

每个缺口必须能从“待处理”进入“完成”或“明确失败”。不能凭最终句子结束位置推断其后的静音已经成功处理；正常任务完成确认也要保存。

没有网络、余额不足或鉴权失败时，保留录音与待处理状态，允许用户修正配置后继续。应用重启后恢复任务清单，用户点击继续后再发起调用。

采集队列溢出、麦克风断开、磁盘写入失败属于本地录音故障：立即明确报告，停止本次录音并保存已有数据。不能静默丢帧后声称录音完整。

## 6. 音视频文件流程

1. 通过文件选择或拖拽导入。至少支持 MP3、WAV、M4A、FLAC、MP4、MKV、MOV、WebM 中 FFmpeg 能解码的常见文件。
2. 使用 ffprobe 检查音轨、采样率、时长；无音轨或文件损坏时给出明确错误。存在多条音轨时列出让用户选择，默认第一条。
3. 提取所选音轨，转换为 16 kHz、单声道、16-bit PCM WAV，并保存在任务目录。[3]
4. 流式读取和写入，避免一次把整部视频或一小时音频载入内存。
5. 生成确定性的片段清单，默认目标 60 秒。优先在边界附近的短停顿切分；停顿判断用简单能量规则或 FFmpeg 即可。
6. 连续讲话没有合适停顿时强制切分，并保留约 1 秒交叠；必须记录每块在原音频中的真实起止采样位置。
7. 每块创建独立 Recognition 实例，在后台调用 `call()`。默认串行请求，完成一块即保存原始结果与进度，再处理下一块。
8. 将服务端相对时间戳加上片段起点，得到全局时间戳。交叠只在相邻片段的交叠区域消除；保留原始返回，防止正常重复用语被误删。
9. 任务成功后显示可编辑全文，支持复制和 TXT 导出。字幕 SRT 导出留待后续版本；第一版不得凭块边界伪造精确句子时间戳。

“仅提取音频”按钮执行前 3 步，随后允许另存 WAV。用户不需要先提取音频再手动上传。

文件任务支持暂停、继续、取消。第一版暂停在当前片段处理完成后生效；取消后停止提交新片段，已完成结果保留。取消和重新运行的任务使用新的运行身份，防止迟到返回覆盖新结果。

若用户在处理文件时开始实时录音，文件任务在当前块完成后自动暂停，实时录音结束后可继续。先保证一个主要工作流程，不追求多个大型任务同时运行。

## 7. 持久化、重试与结果

采用 SQLite 保存会话、音频块、缺口、识别段和任务状态；音频与必要的原始响应保存在独立任务目录。保持单一数据库写入入口，使用事务更新结果与完成状态。

至少保存以下信息，具体表结构由实现者合理设计：

| 对象 | 必需信息 |
| --- | --- |
| 会话 | ID、任务类型、创建时间、音频位置、格式、配置快照、状态、已保存采样数 |
| 片段或缺口 | 稳定 ID、原音频范围、文件路径或偏移、状态、尝试次数、最后错误 |
| 识别结果 | 来源任务/请求、原始文本、最终/临时状态、真实时间戳、用户修改后的文本 |

文本自动导出可采用临时文件加原子替换；最终结果以数据库中已提交的段落为依据重新生成。录音中只允许复制已确认文字；停止且补转写完成后再编辑最终文本，用户修改单独保存，保留原始转写。

首次请求之外，暂时性错误最多自动重试 3 次，采用指数退避并尊重服务端限流提示。网络超时、临时服务错误可重试；密钥无效、余额不足、模型不存在、参数错误应暂停并提示修正。

本地提交必须幂等。远端超时可能已经产生计费，不能承诺重试只计费一次；记录请求 ID 和尝试次数便于定位。

启动时恢复未结束任务：已成功的块不重跑，中断块回到待处理。若有缺口或失败块，导出中保留时间范围占位，例如“[00:12:00–00:13:00 待转写]”，整体状态保持未完成。

## 8. 工程组织与运行交付

建议按职责组织，目录名允许调整，不必为第一版搭建插件平台：

| 路径 | 职责 |
| --- | --- |
| src/asr_client/ui/ | 四个页面、状态显示、信号连接 |
| src/asr_client/audio/ | 录音、重采样、FFmpeg、切片、时间轴 |
| src/asr_client/providers/dashscope_asr.py | 两种调用入口、返回解析、错误归类 |
| src/asr_client/jobs/ | 会话、文件任务、重连、缺口补转写 |
| src/asr_client/storage/ | SQLite、音频存储、配置与密钥 |
| tests/ | 关键状态与故障测试、模拟 ASR |
| scripts/ | Windows 安装与启动脚本 |

建议项目放在 `D:\ASR-Client`，虚拟环境位于项目内。提供 `setup.ps1` 和可双击的 `start.bat`。安装脚本只配置项目环境；Python 不存在时给出清楚的安装方法。FFmpeg 优先复用已有安装，缺失时提供项目内安装方式或明确下载说明，不要求永久修改系统 PATH。

第一版以源码加启动脚本运行作为必要交付。独立 EXE 打包为后续优化，不让打包问题阻塞核心交付。

密钥不得写入仓库、普通配置 JSON、日志、SQLite 或导出文稿。凭据存储不可用时采用仅本次会话模式。日志不记录音频 Base64 和完整请求头。界面明确告知音频将发送到所配置的云服务。

应用关闭后释放麦克风、SDK 连接、FFmpeg 与工作线程；不残留后台录音或持续请求。清理操作仅作用于用户选定任务的应用生成文件，保留原始导入文件。

自写代码可采用 MIT 许可，随附必要的第三方许可说明。客户端不设置会员、订阅或付费功能门槛。

## 9. 实施顺序

| 阶段 | 工作 | 完成标准 |
| --- | --- | --- |
| 1. 最小端到端链路 | 环境、设置、云端适配器、短文件与短麦克风调用 | 有密钥时两种输入都能得到真实结果；缺密钥时完成可独立运行的模拟后端 |
| 2. 文件转写 | 音轨选择、提取、分段、逐段保存、导出 | 60 分钟文件能够完整进入处理流程，界面可操作，内存不随音频总长线性增长 |
| 3. 实时转写 | 连续录音、临时/最终文本、持久化、正常停止 | 连续讲话超 2 分钟，讲话过程中出字，停止后保留最后一句 |
| 4. 可靠性 | 重连、缺口补转写、任务恢复、暂停取消 | 断网及重启场景通过下节测试，无静默丢段与重复提交结果 |
| 5. 交付 | 使用说明、启动脚本、版本锁定、测试报告 | 普通 Windows 用户按说明可启动并配置 API |

每个阶段先让实际流程可运行，再完善界面。API 资料变化由实现者核对和适配，并在交付说明记录。没有用户密钥或音频样本时，先完成所有可做的实现与模拟验证，不伪造真实联调结果。

## 10. 必要验收

把“流程完整性”与“模型识别准确率”分开记录。软件不以模型偶发错字作为丢包判断依据，也不把无异常退出等同于完整转写。

| 场景 | 检查点 |
| --- | --- |
| 连续讲话 3 分钟 | 无两分钟截断；讲话过程中出现结果；临时结果修订不产生重复全文 |
| 60 分钟实时流程 | 无软件时长上限；界面保持响应；音频采样数量与时长一致；无未报告的采集缺口 |
| 停顿后继续讲话 | 静音约 60 秒后仍能继续出字；录音未自动结束 |
| 讲话到最后立即停止 | 最后一句被正常最终化；超时则明确进入待补转写 |
| 中途断网约 30 秒 | 录音继续保存；恢复后新讲话可转写；停止后可补缺口，结果按时间顺序合并 |
| 一小时视频 | 可提取音轨并逐段完成；开头、中间、结尾可核对；原始视频未被修改 |
| 请求重试与乱序事件 | 模拟重复最终事件、旧会话迟到事件、超时重试，最终文稿不重复落入同一段 |
| 关闭后重新打开 | 已落盘录音可恢复；文件任务只继续未完成片段 |
| 本地故障 | 设备断开、音频队列溢出、磁盘失败明确报错，保留已有数据 |
| 无 GPU / 无模型 | 不下载模型，不创建 CUDA 环境；只有云端 ASR 请求 |

对状态、时间轴和断点恢复使用模拟音频/模拟 ASR 自动测试；模拟返回应包含临时修订、重复最终事件、正常重复说话、网络错误和缺少时间戳等情况。

真实联调至少使用短麦克风语音与短文件验证协议。60 分钟实时和文件验收若缺少真实条件，可先通过等长模拟流程验证资源与状态，但报告必须明确列出尚未完成的真实长时测试。用户提供密钥和样本后，按需求完成这些测试，记录模型、SDK 版本、电脑配置、耗时、峰值内存与失败情况。

## 11. 最终交付清单

- 可运行的完整源码、锁定依赖、项目内安装脚本和启动脚本。
- README：从安装、填写 API 到两种输入方式、音频提取和 TXT 导出的最短使用步骤。
- 配置示例：包含地址、模型、目录与可调参数，不包含真实密钥。
- 简短测试报告：分别列出模拟验证、真实 API 验证、未验证项和已知限制。
- 简短维护说明：数据目录、日志位置、如何重试、如何更换同协议模型及更新依赖。

## 12. 官方参考

以下链接用于核验接口与实现细节，工程方案、默认分片长度、恢复策略和验收目标由本任务书确定。

1. [百炼流式 ASR Python SDK：Recognition 的文件与流式入口、回调与结果](https://help.aliyun.com/zh/model-studio/fun-asr-realtime-python-sdk)
2. [百炼流式 ASR WebSocket API](https://help.aliyun.com/zh/model-studio/fun-asr-realtime-websocket-api)
3. [FFmpeg 官方文档](https://ffmpeg.org/ffmpeg.html)
4. [Qt for Python：QThread](https://doc.qt.io/qtforpython-6/PySide6/QtCore/QThread.html)
5. [sounddevice：Raw Streams](https://python-sounddevice.readthedocs.io/en/latest/api/raw-streams.html)
6. [百炼 ASR 模型与音频规格](https://help.aliyun.com/zh/model-studio/asr-model)
7. [百炼 API Key 获取与配置](https://help.aliyun.com/zh/model-studio/get-api-key)
