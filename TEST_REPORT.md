# 第一版测试报告

报告日期：2026-09-10

## 自动模拟验证

执行命令：`.venv\Scripts\python.exe -m pytest`

覆盖范围：

- 地域地址生成与配置原子保存。
- SQLite 会话、片段幂等写入、临时结果最终化、未完成占位和重启恢复。
- 16 kHz 标准 WAV 的流式分段、强制边界交叠和录音元数据恢复。
- 模拟 ASR 文件调用、文件任务断点继续、重复最终事件不重复写入。
- 暂时性错误指数退避、配置类错误停止重试。
- 流式重采样在多次输入之间保持状态，44.1 kHz 与 48 kHz 时长误差控制在一个输出采样附近。

执行环境：Windows、Python 3.12.14。结果：`18 passed`。同时完成 `compileall` 语法检查与 `pip check`，未发现依赖冲突。

无显示界面启动检查已通过：主窗口成功构造、四个页面成功装载并完成一次布局截图检查。已用安装的 DashScope 1.27.4 实际构造 `Recognition` 实例，确认适配器参数与该版本签名一致；该检查没有发送云端请求。

## 60 分钟等长模拟

执行命令：

```powershell
.\.venv\Scripts\python.exe scripts\simulate_long_run.py --minutes 60
.\.venv\Scripts\python.exe scripts\simulate_long_file.py --minutes 60
```

| 流程 | 状态 | 预期/实际采样数 | 分片 | Python 峰值内存 |
| --- | --- | --- | --- | --- |
| 实时录音模拟 | completed | 57,600,000 / 57,600,000 | 连续 100 ms 包 | 0.77 MiB |
| 文件转写模拟 | completed | 57,600,000 / 57,600,000 | 66 | 0.69 MiB |

两个脚本均使用确定性模拟 ASR，没有访问麦克风或云端。模拟文件为静音 WAV，切片器会在目标边界附近选择最早的等能量静音窗口，因此产生 66 个片段。临时目录在脚本退出后清理。

## 真实 API 验证

尚未执行。当前工作区没有用户 API Key、麦克风语音授权或媒体样本，因此未伪造真实联调结果。

待验证项：

- 短麦克风：实际首字延迟、停止后的最后一句、request ID。
- 短文件：`Recognition.call()` 在用户账号与所选模型上的返回结构。
- 连续讲话 3 分钟、静音 60 秒后继续、断网约 30 秒。
- 60 分钟实时录音和一小时视频的完整流程、耗时与峰值内存。

## 已知风险

- 百炼账号的模型开通状态、地域和计费余额只能在用户环境实际确认。
- 不同声卡是否接受 16 kHz 存在差异；客户端会回退到设备默认采样率并进行有状态重采样。
- SDK 的 `stop()` 可能受网络影响阻塞；客户端等待 15 秒后将尾部标成待补转写，底层网络线程可能稍后自行退出。
